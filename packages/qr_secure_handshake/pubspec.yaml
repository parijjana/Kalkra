name: qr_secure_handshake
description: A Dart library for securing LAN handshakes via QR-embedded secrets and payload encryption.
version: 1.0.0
homepage: https://github.com/kalkra/qr_secure_handshake
publish_to: 'none' # For now, set to none to prevent accidental publishing

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  encrypt: ^5.0.3

dev_dependencies:
  test: ^1.24.0
