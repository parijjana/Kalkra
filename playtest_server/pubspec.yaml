name: playtest_server
description: A lightweight Dart Shelf server for Kalkra playtest results.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_static: ^1.1.2
  sqlite3: ^2.5.0
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
